import { style } from '@vanilla-extract/css';

const NestStyles = style({
  margin: 'var(--margin)',
  padding: 'var(--padding)',
  border: 'var(--border)',
  background: 'var(--background)',
  maxWidth: 'var(--maxWidth)',
})

export { NestStyles };
